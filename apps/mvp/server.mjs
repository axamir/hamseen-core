import {createServer} from 'node:http';import {readFile,stat,mkdir} from 'node:fs/promises';import {join,normalize,extname,fileURLToPath} from 'node:path';import {createCore} from './core.mjs';
const root=fileURLToPath(new URL('.',import.meta.url)),pub=join(root,'public'),data=join(root,'data');await mkdir(data,{recursive:true});const core=createCore(join(data,'hamseen.sqlite'));core.seed();
const json=(res,status,body)=>{res.writeHead(status,{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'});res.end(JSON.stringify(body))};
const body=async req=>{let raw='';for await(const chunk of req){raw+=chunk;if(raw.length>1e6)throw Object.assign(new Error('large'),{status:413,code:'PAYLOAD_TOO_LARGE'})}return raw?JSON.parse(raw):{}};
const ctx=req=>core.context(req.headers['x-demo-user']||'resident-1',req.headers['x-request-id']);
async function api(req,res,url){const p=url.pathname,method=req.method;if(method==='GET'&&p==='/api/v1/hub')return json(res,200,{data:core.hub(ctx(req))});if(method==='POST'&&p==='/api/v1/guest-invites')return json(res,201,{data:core.createGuest(ctx(req),await body(req))});let m=p.match(/^\/api\/v1\/guest-invites\/([^/]+)\/transition$/);if(method==='POST'&&m){const b=await body(req);return json(res,200,{data:core.transitionGuest(ctx(req),m[1],b.status,b.expected_version)})}if(method==='POST'&&p==='/api/v1/tickets')return json(res,201,{data:core.createTicket(ctx(req),await body(req))});m=p.match(/^\/api\/v1\/tickets\/([^/]+)\/transition$/);if(method==='POST'&&m)return json(res,200,{data:core.transitionTicket(ctx(req),m[1],await body(req))});if(method==='POST'&&p==='/api/v1/bookings')return json(res,201,{data:core.createBooking(ctx(req),await body(req))});if(method==='POST'&&p==='/api/v1/receipts')return json(res,201,{data:core.submitReceipt(ctx(req),await body(req))});m=p.match(/^\/api\/v1\/receipts\/([^/]+)\/review$/);if(method==='POST'&&m){const b=await body(req);return json(res,200,{data:core.reviewReceipt(ctx(req),m[1],b.status)})}if(method==='GET'&&p==='/api/v1/audit')return json(res,200,{data:core.auditLog(ctx(req))});return json(res,404,{error:{code:'NOT_FOUND',message:'Not found'}})}
export const server=createServer(async(req,res)=>{try{const url=new URL(req.url,'http://local');if(url.pathname.startsWith('/api/'))return await api(req,res,url);const rel=url.pathname==='/'?'index.html':url.pathname.replace(/^\//,'');const path=normalize(join(pub,rel));if(!path.startsWith(pub))throw Object.assign(new Error(),{status:403,code:'FORBIDDEN'});if(!(await stat(path)).isFile())throw new Error();const types={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8'};res.writeHead(200,{'Content-Type':types[extname(path)]||'application/octet-stream'});res.end(await readFile(path));}catch(e){json(res,e.status||500,{error:{code:e.code||'INTERNAL_ERROR',message:e.message||'Error',request_id:req.headers['x-request-id']||null}})}});
if(process.env.NODE_ENV!=='test')server.listen(Number(process.env.PORT||4180),'127.0.0.1',()=>console.log('Hamseen MVP: http://127.0.0.1:4180'));
